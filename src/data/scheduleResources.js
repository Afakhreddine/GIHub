// Repo-managed Schedule tab resources.
// Populated by Hermes when Ali provides a lecture schedule image, then incrementally maintained by weekly schedule-topic screening.

const scheduleResources = {};

export default scheduleResources;
